{
  "name": "zewei-os-skill-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "泽玮 OS 技能、插件与代理注册仓库",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate && npm test"
  }
}
