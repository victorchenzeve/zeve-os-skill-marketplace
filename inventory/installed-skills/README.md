# 已安装技能盘点

记录已安装技能的来源、版本和评估状态。使用 `templates/inventory-record.md`，每个技能一份 Markdown 记录。

这里不保存安装包，也不后台自动扫描或同步本机目录；仅在用户明确请求盘点时生成记录。记录存在不表示已纳管。

当前盘点见 [INDEX.md](INDEX.md)。逐条 Markdown 记录存放在对应快照的 `records/` 目录，避免将视图重叠误当作多份资产。
