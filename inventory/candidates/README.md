# 候选治理队列

`queue.json` 由最近一次飞书只读审计自动生成，把重复、遗漏、仅表内记录和运行时加载错误转成可追踪动作。

队列不代表 Skill 的来源、许可、质量或正式纳管结论。处理任何条目前必须回到原始来源核实；未经明确授权不修改飞书表，不修改、合并、删除或安装原 Skill。

运行 `npm run candidates:build` 重建队列，运行 `npm run candidates:check` 检查审计与队列是否漂移。
