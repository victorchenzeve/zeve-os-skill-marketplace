---
name: knowledge-freshness-auditor
description: Audit a knowledge collection for stale, superseded, undated, conflicting, or unreachable sources and prioritize refresh work.
license: MIT
---

# 知识时效审计

## 触发条件

当用户需要“审计知识集合中的过期、被替代、无日期、冲突或不可访问来源并确定更新优先级”时使用。若输入来源、权限或任务目标不满足边界要求，先说明缺口并采用安全的降级输出。

## 输入

需要知识记录清单、来源、版本或日期、适用范围、使用频率和关键性。

## 执行步骤

1. 定义各类知识的时效窗口和高风险变更触发条件。
2. 检查日期、版本、来源可访问性和被替代关系。
3. 识别冲突记录、无主记录和高使用但低可信条目。
4. 按业务影响、暴露频率和修复成本排序。
5. 形成更新、归档、合并或继续观察的建议。

## 输出

输出时效审计表，包含状态、证据、影响、优先级、建议动作、负责人和复查日期。

## 权限与边界

不自动删除、覆盖或重新发布知识，不把“较旧”直接等同于“错误”。

## 失败处理

缺少日期时标为待核实而非过期；来源不可访问时记录观察事实与复验方式。

## 验证方法

使用成功、缺失输入和边界风险三类合成用例验证；逐项检查输出结构、事实边界和失败降级。证据保存在 `evals/knowledge-freshness-auditor/`。
