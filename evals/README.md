# 行为评估

结构测试通过不代表技能实际可用。每个技能在 `evals/<skill-id>/` 保存用例和执行记录，可从 `case-template.md` 开始。

至少覆盖正常输入、缺失或错误输入、依赖不可用或权限边界三类情况。记录资产版本、环境、输入、预期、实际输出、结论和证据；使用合成或脱敏输入，禁止保存凭据。

首发 4 个 Skill 已完成合成成功、缺失输入和边界用例，因此成熟度为 `experimental`。升级为 `stable` 前仍需保留真实业务验证、失败条件和依赖故障证据。成熟度条件见 `governance/MATURITY_MODEL.md`。
